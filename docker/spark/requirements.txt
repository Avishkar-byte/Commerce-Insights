# Pinned so a rebuild before the viva produces the same image.
# These are the versions the validated pipeline run used.
pyspark==3.5.9
pymongo==4.18.1
pyyaml==6.0.3
kaggle==2.2.4
pandas==3.0.5
pyarrow==25.0.1
pytest==9.1.1
ruff==0.16.8
jupyterlab==4.6.3
matplotlib==3.11.2
